import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calculadora.perimetros',
  appName: 'calculadora-perimetros',
  webDir: 'www'
};

export default config;
